name: Deploy Discord Bot

on:
  push:
    branches:
      - main
    paths:
      - "apps/discord-bot/**"
      - "packages/typescript-config/**"
      - "package.json"
      - "pnpm-lock.yaml"
      - "pnpm-workspace.yaml"
      - ".github/workflows/deploy-discord-bot.yml"
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: deploy-discord-bot-${{ github.ref }}
  cancel-in-progress: true

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 22
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile --ignore-scripts

      - name: Typecheck Discord bot
        run: pnpm --filter discord-bot typecheck

      - name: Build Discord bot
        run: pnpm --filter discord-bot build

      - name: Setup Flyctl
        uses: superfly/flyctl-actions/setup-flyctl@master

      # Scope the Fly deploy token to the deploy step only so the install,
      # typecheck, and build steps cannot read or exfiltrate it.
      - name: Deploy to Fly.io
        run: flyctl deploy --remote-only --config apps/discord-bot/fly.toml --app median-discord-bot --ha=false .
        env:
          FLY_API_TOKEN: ${{ secrets.FLY_API_TOKEN }}
